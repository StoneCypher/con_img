{
  "name": "con_img",
  "version": "0.3.0",
  "description": "Render images in the developers' console",
  "main": "dist/con_img.js",
  "scripts": {
    "removedir": "rimraf build -f && rimraf dist -f && rimraf docs -f",
    "createdir": "mkdir build && mkdir dist && mkdir docs && cd docs && mkdir docs && cd ..",
    "clean": "npm run removedir && rm -f src/js/jssm-dot.js && npm run createdir",
    "babel": "babel src/js -d build/",
    "pack": "browserify -r ./build/con_img.es5.js:jssm -o build/con_img.es5.cjs.js",
    "build": "npm run clean && npm run babel",
    "test": "echo \"Error: no test specified\" && exit 1",
    "travis": "rm -f src/js/jssm-dot.js && npm run coverage && nyc report --reporter=text-lcov | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/StoneCypher/con_img.git"
  },
  "keywords": [
    "image",
    "console",
    "img",
    "developer",
    "stonecypher",
    "utility",
    "con",
    "repl"
  ],
  "author": "John Haugeland <stonecypher@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/StoneCypher/con_img/issues"
  },
  "homepage": "https://github.com/StoneCypher/con_img#readme",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-transform-flow-strip-types": "^6.22.0",
    "babel-preset-es2015": "^6.24.1",
    "browserify": "^14.4.0",
    "rimraf": "^2.6.1"
  }
}
